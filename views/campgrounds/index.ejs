<% include ../partials/header %>

<!-- PARRALLAX -->
<div class="jumbotron paral paralsec">
        <h1 class="display-3">Yelp Camp</h1>
        <p class="lead">Hand-picked by our users</p>
        <p class="lead">
        <a class="btn btn-primary btn-lg" href="/campgrounds/new" role="button">Add Your Favorite Campground</a>
        </p>
    </div>
    <!-- END PARRALLAX -->

<div class="container">
    <!-- <header class="jumbotron">
        <div class="container">
            <h1>YelpCamp</h1>
            <p>View our hand-picked campgrounds from all over the world</p>
            <p>
                <a class="btn btn-primary btn-large" href="/campgrounds/new">Add New Campgroud</a>
            </p>
        </div>
    </header> -->

    <div class="row">
        <div class="col-lg-12">
            <h3>Our Most Popular Campgrounds!</h3>
        </div>
    </div>
    <div class="row text-center" style="display:flex; flex-wrap: wrap;">
        <% campgrounds.forEach(function(campground){ %>
            <div class="col-md-3 col-sm-6">
                <div class="thumbnail">
                <img src="<%=campground.image%>"/>
                <div class="caption">
                    <h4><%= campground.name %></h4>
                </div>
                <p>
                    <%= campground.description %>
                </p>
                <a class="btn btn-primary" href="/campgrounds/<%= campground._id %>"" role="button">Show More</a>
                </div>
            </div>
    <% }); %>
    </div>
</div>

<% include ../partials/footer %>
